import { LoginForm } from "@/components/auth/LoginForm";

export const metadata = {
  title: "Sign In | NEXTGEN FITNESS",
  description: "Sign in to your NEXTGEN FITNESS member or staff account.",
};

export default function LoginPage() {
  return <LoginForm />;
}
